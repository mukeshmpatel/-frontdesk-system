import{notFound}from"next/navigation";import{websitePreview}from"../../../db/website-factory";import{getChatGPTUser}from"../../chatgpt-auth";import"./public-site.css";
function safeBuilderHtml(value:unknown){return String(value||"").replace(/<script[\s\S]*?<\/script>/gi,"").replace(/\son\w+\s*=\s*(["']).*?\1/gi,"").replace(/javascript\s*:/gi,"")}
function safeBuilderCss(value:unknown){return String(value||"").replace(/<\/?style/gi,"").replace(/@import/gi,"").replace(/javascript\s*:/gi,"")}
export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,user=await getChatGPTUser(),site=await websitePreview(slug,user?.email);if(!site)notFound();const c=site.content;return <main className="public-hotel">{site.status!=="PUBLISHED"&&<div className="draft-preview-banner"><strong>Private draft preview</strong><span>This website is not public. Complete missing information and publish from AAIQ Website Factory.</span><a href="/aaiq-website-factory">Return to Website Factory</a></div>}{c.builderHtml?<><style dangerouslySetInnerHTML={{__html:safeBuilderCss(c.builderCss)}}/><div className="grapes-published-site" dangerouslySetInnerHTML={{__html:safeBuilderHtml(c.builderHtml)}}/></>:<><nav><strong>{c.brandName}</strong><div><a href="#rooms">Rooms</a><a href="#amenities">Amenities</a><a href="#meetings">Meetings</a><a href="#contact">Contact</a></div><a className="book" href={c.bookingUrl||"#contact"}>Book now</a></nav><header><small>{c.eyebrow}</small><h1>{c.heroTitle}</h1><p>{c.heroDescription}</p><div><a className="book" href={c.bookingUrl||"#contact"}>Check availability</a><span>{c.phone||"Phone number pending verification"}</span></div></header><section className="proof"><article><strong>{c.roomCount}</strong><span>Guest rooms</span></article><article><strong>{c.roomTypes.length}</strong><span>Room experiences</span></article><article><strong>{c.amenities.length}</strong><span>On-property amenities</span></article></section><section id="rooms"><small>STAY YOUR WAY</small><h2>Rooms designed for a comfortable Salina stay</h2><div className="site-cards">{c.roomTypes.map((room:string)=><article key={room}><span>ROOM TYPE</span><h3>{room}</h3><p>Comfortable accommodations with essential in-room amenities and direct hotel support.</p></article>)}</div></section><section id="amenities" className="dark"><small>AT THE HOTEL</small><h2>Everything you need, close at hand</h2><div className="amenity-list">{c.amenities.map((a:string)=><span key={a}>{a}</span>)}</div></section>{c.sections.meetings&&<section id="meetings"><small>MEET & CELEBRATE</small><h2>Flexible space for conferences, meetings and celebrations</h2><p>Contact our team for availability, room layouts, catering options and a customized event proposal.</p></section>}<footer id="contact"><div><strong>{c.brandName}</strong><span>{c.address}</span><span>{c.phone}</span></div><a className="book" href={c.bookingUrl||"#contact"}>Reserve directly</a></footer></>}</main>}
