import {getChatGPTUser} from "../../../chatgpt-auth";import {ensureStaffWorkspace} from "../../../../db/staff";
import {accessCenter,requestRecovery,updateAccessUser} from "../../../../db/access-management";
export async function GET(){const user=await getChatGPTUser();if(!user)return Response.json({error:"Sign in is required."},{status:401});await ensureStaffWorkspace(user.email,user.displayName);const result=await accessCenter(user.email);return result?Response.json(result):Response.json({error:"Staff access required."},{status:403})}
export async function PATCH(request:Request){const user=await getChatGPTUser();if(!user)return Response.json({error:"Sign in required."},{status:401});try{const body=await request.json() as any;const result=await updateAccessUser(user.email,body.email||"",body);return result?Response.json(result):Response.json({error:"Administrator access required."},{status:403})}catch(error){return Response.json({error:error instanceof Error?error.message:"Update failed."},{status:400})}}
export async function POST(request:Request){const user=await getChatGPTUser();if(!user)return Response.json({error:"Sign in required."},{status:401});try{const body=await request.json() as any;const result=await requestRecovery(user.email,body.email||user.email,body.recoveryType,body.channel);return result?Response.json(result,{status:202}):Response.json({error:"Access denied."},{status:403})}catch(error){return Response.json({error:error instanceof Error?error.message:"Recovery request failed."},{status:400})}}
