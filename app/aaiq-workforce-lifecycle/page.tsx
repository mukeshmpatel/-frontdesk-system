import{requireChatGPTUser}from"../chatgpt-auth";import{ensureStaffWorkspace}from"../../db/staff";import{hasModuleAccess}from"../../db/access-management";import AaiqAccessDenied from"../components/aaiq-access-denied";import WorkforceLifecycleClient from"./workforce-lifecycle-client";import"./workforce-lifecycle.css";
export const dynamic="force-dynamic";export default async function Page(){const user=await requireChatGPTUser("/aaiq-workforce-lifecycle");await ensureStaffWorkspace(user.email,user.displayName);if(!await hasModuleAccess(user.email,"AAIQ_WORKFORCE_LIFECYCLE"))return <AaiqAccessDenied moduleName="AAIQ Hiring & Workforce Lifecycle"/>;return <WorkforceLifecycleClient/>}
