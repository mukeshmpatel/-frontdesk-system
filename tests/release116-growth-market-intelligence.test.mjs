import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";const read=path=>readFile(new URL(`../${path}`,import.meta.url),"utf8");
test("growth intelligence combines active property facts with sourced market conditions",async()=>{const[service,client]=await Promise.all([read("db/growth-platform.ts"),read("app/aaiq-website-factory/growth-command-center.tsx")]);assert.match(service,/property_id=\?/);assert.match(service,/api\.weather\.gov\/points/);assert.match(service,/National Weather Service/);assert.match(service,/AAIQ canonical report facts/);assert.match(client,/Current property trend vs sourced market conditions/);assert.match(client,/Data source health/);});
test("external source failures degrade honestly and never fabricate market data",async()=>{const service=await read("db/growth-platform.ts");assert.match(service,/TEMPORARILY_UNAVAILABLE/);assert.match(service,/CREDENTIAL_REQUIRED/);assert.match(service,/external intelligence is explicitly degradable/);assert.doesNotMatch(service,/Math\.random/);});
