import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";const read=path=>readFile(new URL(`../${path}`,import.meta.url),"utf8");
test("Website Factory preserves twenty selectable hotel designs",async()=>{const[service,client]=await Promise.all([read("db/website-factory.ts"),read("app/aaiq-website-factory/website-factory-client.tsx")]);const ids=[...service.matchAll(/\{id:"[A-Z_]+",name:/g)];assert.equal(ids.length,20);assert.match(service,/templates: WEBSITE_TEMPLATES/);assert.match(client,/Choose a starting design/);assert.match(client,/templateId/);});
test("website description is generated while user input stays property specific",async()=>{const[service,client]=await Promise.all([read("db/website-factory.ts"),read("app/aaiq-website-factory/website-factory-client.tsx")]);assert.match(service,/generatedDescription/);assert.match(service,/selectedTemplate\.tone/);assert.match(client,/Leave blank and AAIQ will prepare location-aware hotel copy automatically/);for(const field of["Public phone","Public address","Direct booking URL"])assert.match(client,new RegExp(field));});
